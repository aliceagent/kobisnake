// Fixture: an object literal's `label` property holding a raw string instead of a catalogue reference — the
// same shape mainMenu.js's MENU_ITEMS used before KI-20-02 migrated it to `label: menu.itemReplay` etc.
export const MENU_ITEMS = [{ label: 'A brand new hardcoded menu row', event: 'SELECT_NEW' }];
