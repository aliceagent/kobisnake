// Fixture: identical to clean-exemption-comment.js with the `eslint-disable-next-line` stripped — must fail.
export const COLOUR_NOTE_COPY = {
  suggestion: (colorName) => `Try ${colorName.toUpperCase()} for player 2.`,
};
